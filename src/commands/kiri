import Command from '../lib/structures/Command';
import MessageEmbed from '../lib/structures/MessageEmbed';

export default new Command('kiri', async (caller, cmd) => {
	const helpEmbed = new MessageEmbed()
		.setTitle('`Mensaje para chat general`')
		.setDescription('Callaros <@605747883601428520> y <@621699093860843540>');
	return caller.utils.discord.createMessage(cmd.channel.id, { embed: helpEmbed.code });
},
{
	level: 'REGULAR',
	aliases: []
});